{
  "name": "paintart-clock",
  "version": "1.0.0",
  "private": true,
  "description": "A digital clock on a rotating wall of public-domain paintings.",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'",
    "validate:catalog": "node scripts/validate-catalog.mjs",
    "build:catalog": "node scripts/build-catalog.mjs",
    "check": "npm run validate:catalog && npm test",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
